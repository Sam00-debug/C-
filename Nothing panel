local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TextChatService = game:GetService("TextChatService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local displayName = player.DisplayName or player.Name
local guiName = "NothingPanelUI"

-- Clean up previous instances
local targetParent = player:WaitForChild("PlayerGui")
if targetParent:FindFirstChild(guiName) then
    targetParent[guiName]:Destroy()
end

--=========================================
-- 1. BASE SYSTEM & SOUND CORE
--=========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = guiName
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true -- Covers screen entirely for the intro
ScreenGui.Parent = targetParent

-- Audio Setup
local ClickSound = Instance.new("Sound")
ClickSound.SoundId = "rbxassetid://876939830"
ClickSound.Volume = 1
ClickSound.Parent = ScreenGui

local IntroSound = Instance.new("Sound")
IntroSound.SoundId = "rbxassetid://755895"
IntroSound.Volume = 1
IntroSound.Parent = ScreenGui

local function PlayClick()
    ClickSound:Play()
end

--=========================================
-- 2. BACKGROUND TASKS (CHAT & REMOTES)
--=========================================
task.spawn(function()
    -- Chat Message
    pcall(function()
        local textChannel = TextChatService:WaitForChild("TextChannels", 10):WaitForChild("RBXGeneral", 10)
        if textChannel then
            textChannel:SendAsync("system:- sorry , panel source code platform went under maintenance, we don't have access to Scripts. let's build panel again 😼")
        end
    end)
end)

task.spawn(function()
    -- RP Remotes
    pcall(function()
        local reFolder = ReplicatedStorage:WaitForChild("RE", 10)
        local colorRemote = reFolder:WaitForChild("1RPNam1eColo1r", 10)
        local textRemote = reFolder:WaitForChild("1RPNam1eTex1t", 10)
        
        local redColor = Color3.new(1, 0, 0)
        
        local bioColorArgs = {"PickingRPBioColor", redColor}
        colorRemote:FireServer(unpack(bioColorArgs))

        local nameColorArgs = {"PickingRPNameColor", redColor}
        colorRemote:FireServer(unpack(nameColorArgs))

        local nameTextArgs = {"RolePlayName", "_N0Th1nG_P4n3l"}
        textRemote:FireServer(unpack(nameTextArgs))

        local bioTextArgs = {"RolePlayBio", "_" .. displayName .. "_"}
        textRemote:FireServer(unpack(bioTextArgs))
    end)
end)

--=========================================
-- 3. CROSS-PLATFORM DRAGGING
--=========================================
local function MakeDraggable(guiObject, dragArea)
    dragArea = dragArea or guiObject
    local dragging, dragInput, dragStart, startPos

    dragArea.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = guiObject.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    dragArea.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            guiObject.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X, 
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
end

--=========================================
-- 4. PROFESSIONAL DARK UI CONSTRUCTION
--=========================================
-- MAIN APP WINDOW
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0.4, 0, 0.55, 0)
MainFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- TOPBAR
local Topbar = Instance.new("Frame")
Topbar.Size = UDim2.new(1, 0, 0, 35)
Topbar.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
Topbar.BorderSizePixel = 0
Topbar.Parent = MainFrame

Instance.new("UICorner", Topbar).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -80, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "Nothing Panel"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 15
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Topbar

-- WINDOW CONTROLS
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -35, 0, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 75, 75)
CloseBtn.TextSize = 16
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = Topbar

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 35, 0, 35)
MinimizeBtn.Position = UDim2.new(1, -70, 0, 0)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.TextSize = 20
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Parent = Topbar

-- CONFIRMATION OVERLAY
local ConfirmFrame = Instance.new("Frame")
ConfirmFrame.Size = UDim2.new(1, 0, 1, 0)
ConfirmFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
ConfirmFrame.BackgroundTransparency = 0.3
ConfirmFrame.BorderSizePixel = 0
ConfirmFrame.Visible = false
ConfirmFrame.ZIndex = 10
ConfirmFrame.Parent = MainFrame
Instance.new("UICorner", ConfirmFrame).CornerRadius = UDim.new(0, 8)

local ConfirmBox = Instance.new("Frame")
ConfirmBox.Size = UDim2.new(0.7, 0, 0.4, 0)
ConfirmBox.Position = UDim2.new(0.15, 0, 0.3, 0)
ConfirmBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
ConfirmBox.BorderSizePixel = 0
ConfirmBox.ZIndex = 11
ConfirmBox.Parent = ConfirmFrame
Instance.new("UICorner", ConfirmBox).CornerRadius = UDim.new(0, 8)

local PromptText = Instance.new("TextLabel")
PromptText.Size = UDim2.new(1, 0, 0.5, 0)
PromptText.BackgroundTransparency = 1
PromptText.Text = "Do you want to close script?"
PromptText.TextColor3 = Color3.fromRGB(255, 255, 255)
PromptText.Font = Enum.Font.GothamBold
PromptText.TextSize = 14
PromptText.ZIndex = 11
PromptText.Parent = ConfirmBox

local YesBtn = Instance.new("TextButton")
YesBtn.Size = UDim2.new(0.4, 0, 0.35, 0)
YesBtn.Position = UDim2.new(0.08, 0, 0.55, 0)
YesBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
YesBtn.Text = "Yes"
YesBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
YesBtn.Font = Enum.Font.GothamBold
YesBtn.TextSize = 13
YesBtn.ZIndex = 11
YesBtn.Parent = ConfirmBox
Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0, 6)

local NoBtn = Instance.new("TextButton")
NoBtn.Size = UDim2.new(0.4, 0, 0.35, 0)
NoBtn.Position = UDim2.new(0.52, 0, 0.55, 0)
NoBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
NoBtn.Text = "No"
NoBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
NoBtn.Font = Enum.Font.GothamBold
NoBtn.TextSize = 13
NoBtn.ZIndex = 11
NoBtn.Parent = ConfirmBox
Instance.new("UICorner", NoBtn).CornerRadius = UDim.new(0, 6)

-- SCROLL LIST
local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Size = UDim2.new(1, -20, 1, -50)
ScrollFrame.Position = UDim2.new(0, 10, 0, 45)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 4
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 8)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Parent = ScrollFrame

-- REOPEN BUTTON
local ReopenBtn = Instance.new("TextButton")
ReopenBtn.Size = UDim2.new(0, 50, 0, 50)
ReopenBtn.Position = UDim2.new(0, 20, 0.5, -25)
ReopenBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
ReopenBtn.Text = "NP"
ReopenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ReopenBtn.TextSize = 14
ReopenBtn.Font = Enum.Font.GothamBold
ReopenBtn.Visible = false
ReopenBtn.Parent = ScreenGui
Instance.new("UICorner", ReopenBtn).CornerRadius = UDim.new(0, 25)

--=========================================
-- 5. KEY AUTHENTICATION SYSTEM
--=========================================
local KeyFrame = Instance.new("Frame")
KeyFrame.Size = UDim2.new(0, 320, 0, 240)
KeyFrame.Position = UDim2.new(0.5, -160, 0.5, -120)
KeyFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
KeyFrame.BorderSizePixel = 0
KeyFrame.Visible = false
KeyFrame.Parent = ScreenGui

Instance.new("UICorner", KeyFrame).CornerRadius = UDim.new(0, 12)

local KeyCloseBtn = Instance.new("TextButton")
KeyCloseBtn.Size = UDim2.new(0, 30, 0, 30)
KeyCloseBtn.Position = UDim2.new(1, -35, 0, 5)
KeyCloseBtn.BackgroundTransparency = 1
KeyCloseBtn.Text = "X"
KeyCloseBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
KeyCloseBtn.Font = Enum.Font.GothamBold
KeyCloseBtn.TextSize = 18
KeyCloseBtn.Parent = KeyFrame

local EmojiLabel = Instance.new("TextLabel")
EmojiLabel.Size = UDim2.new(0, 60, 0, 60)
EmojiLabel.Position = UDim2.new(0.5, -30, 0, 15)
EmojiLabel.BackgroundTransparency = 1
EmojiLabel.Text = "🤖"
EmojiLabel.TextScaled = true
EmojiLabel.Parent = KeyFrame

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Size = UDim2.new(1, 0, 0, 25)
KeyTitle.Position = UDim2.new(0, 0, 0, 75)
KeyTitle.BackgroundTransparency = 1
KeyTitle.Text = "Mecha Authentication"
KeyTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTitle.Font = Enum.Font.GothamBold
KeyTitle.TextSize = 18
KeyTitle.Parent = KeyFrame

local NoticeLabel = Instance.new("TextLabel")
NoticeLabel.Size = UDim2.new(0.9, 0, 0, 45)
NoticeLabel.Position = UDim2.new(0.05, 0, 0, 100)
NoticeLabel.BackgroundTransparency = 1
NoticeLabel.Text = "password- 'Mob_One0' "
NoticeLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
NoticeLabel.Font = Enum.Font.GothamSemibold
NoticeLabel.TextSize = 13
NoticeLabel.TextWrapped = true
NoticeLabel.Parent = KeyFrame

local KeyInput = Instance.new("TextBox")
KeyInput.Size = UDim2.new(0.8, 0, 0, 35)
KeyInput.Position = UDim2.new(0.1, 0, 0, 145)
KeyInput.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderText = "Enter Access Key..."
KeyInput.Font = Enum.Font.Gotham
KeyInput.TextSize = 14
KeyInput.Text = ""
KeyInput.Parent = KeyFrame
Instance.new("UICorner", KeyInput).CornerRadius = UDim.new(0, 6)

local AccessBtn = Instance.new("TextButton")
AccessBtn.Size = UDim2.new(0.8, 0, 0, 35)
AccessBtn.Position = UDim2.new(0.1, 0, 0, 190)
AccessBtn.BackgroundColor3 = Color3.fromRGB(30, 215, 96)
AccessBtn.Text = "Verify Key"
AccessBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
AccessBtn.Font = Enum.Font.GothamBold
AccessBtn.TextSize = 16
AccessBtn.Parent = KeyFrame
Instance.new("UICorner", AccessBtn).CornerRadius = UDim.new(0, 6)

MakeDraggable(MainFrame, Topbar)
MakeDraggable(KeyFrame)
MakeDraggable(ReopenBtn)

--=========================================
-- 6. INTRO SEQUENCE (5 SEC FADE)
--=========================================
local IntroScreen = Instance.new("Frame")
IntroScreen.Size = UDim2.new(1, 0, 1, 0)
IntroScreen.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
IntroScreen.ZIndex = 999
IntroScreen.Parent = ScreenGui

IntroSound:Play()
task.wait(0.5)

local fadeTween = TweenService:Create(IntroScreen, TweenInfo.new(1), {BackgroundTransparency = 1})
fadeTween:Play()
fadeTween.Completed:Wait()
IntroScreen:Destroy()
KeyFrame.Visible = true -- Reveal Key System after intro

--=========================================
-- 7. PAYLOAD BUTTON POPULATION
--=========================================
local buttonsData = {
    {"under maintenance have to wait💔🥀", "https://raw.githubusercontent.com/Sam00-debug/Nothing-Panel-/refs/heads/main/Notice"},
    

{"prop🎁","https://raw.githubusercontent.com/Sam00-debug/Nothing-Panel-/refs/heads/main/Prop%20script"},
{"chat💬","https://raw.githubusercontent.com/Sam00-debug/Nothing-chat/refs/heads/main/Script"},
{"infinite yield🛠️ ","https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"},
{"discord 🔗","https://raw.githubusercontent.com/Sam00-debug/Nothing-Panel-/refs/heads/main/Dc%20link"}




}

for _, data in ipairs(buttonsData) do
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -10, 0, 35)
    Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    Btn.Text = data[1]
    Btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    Btn.TextSize = 12
    Btn.Font = Enum.Font.GothamSemibold
    Btn.Parent = ScrollFrame

    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)
    
    Btn.MouseButton1Click:Connect(function()
        PlayClick()
        TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(60, 60, 75)}):Play()
        
        task.spawn(function()
            pcall(function() loadstring(game:HttpGet(data[2]))() end)
        end)
        
        task.wait(0.1)
        TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(45, 45, 55)}):Play()
    end)
end

--=========================================
-- 8. GLOBAL BUTTON SOUNDS & LOGIC
--=========================================
local sysButtons = {AccessBtn, KeyCloseBtn, MinimizeBtn, ReopenBtn, CloseBtn, YesBtn, NoBtn}
for _, btn in ipairs(sysButtons) do
    btn.MouseButton1Click:Connect(PlayClick)
end

AccessBtn.MouseButton1Click:Connect(function()
    if KeyInput.Text == "Mob_One0" then
        KeyFrame.Visible = false
        MainFrame.Visible = true
    else
        AccessBtn.Text = "Invalid Key!"
        AccessBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
        task.wait(1.5)
        AccessBtn.Text = "Verify Key"
        AccessBtn.BackgroundColor3 = Color3.fromRGB(30, 215, 96)
    end
end)

KeyCloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    ReopenBtn.Visible = true
end)

ReopenBtn.MouseButton1Click:Connect(function()
    ReopenBtn.Visible = false
    MainFrame.Visible = true
end)

CloseBtn.MouseButton1Click:Connect(function() ConfirmFrame.Visible = true end)
NoBtn.MouseButton1Click:Connect(function() ConfirmFrame.Visible = false end)
YesBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)


